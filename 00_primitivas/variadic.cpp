#include<stdio.h>
#include<stdlib.h>

int main(){
	int operando1 = 10,
	    operando2;

	system("toilet --metal -fpagga suma");

	printf("operando 2: ");
	scanf("%i", &operando2);

	printf("%i + %i = %i\n", operando1, operando2, operando1 + operando2);

	return EXIT_SUCCESS;
}
